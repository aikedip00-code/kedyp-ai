// Agrega aquí únicamente testimonios verificados con: nombre, empresa, puesto,
// testimonio, fotografía/avatar y video opcional. La sección permanece oculta
// hasta contar con contenido real aprobado para publicación.
const verifiedTestimonials: ReadonlyArray<never> = []

export function TestimonialsSection() {
  if (verifiedTestimonials.length === 0) return null
  return null
}
